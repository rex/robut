<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string>MIT License</string>

	<!-- Menubar-only: no Dock icon, no main window, no app switcher entry. -->
	<key>LSUIElement</key>
	<true/>

	<!-- Sparkle auto-update. The appcast is published as a GitHub Release
	     asset; the /releases/latest/download/ URL always resolves to the
	     newest release, so the feed needs no hosting of its own. Updates
	     must carry a valid EdDSA signature from the key below AND the same
	     Developer ID as the running app. The private key lives only in the
	     maintainer's login keychain (`make sparkle-keys-init`). -->
	<key>SUFeedURL</key>
	<string>https://github.com/rex/robut/releases/latest/download/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>bJ2SRh3KHrwfSUb63+79Jdr5+/6RuJG5kmy5LllW4Y4=</string>
	<key>SUEnableAutomaticChecks</key>
	<true/>

	<!-- Robut talks only to provider APIs over HTTPS. No ATS exceptions. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<false/>
	</dict>
</dict>
</plist>
